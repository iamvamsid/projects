# Secrets — NEVER commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Corpus / build artifacts (kept local)
data/*
!data/.gitkeep
storage/      # persisted vector index (regenerable via `python -m src.ingest`)

# OS / editor
.DS_Store
.idea/
.vscode/
